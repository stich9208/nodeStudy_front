import React from "react";
import styled from "styled-components";

const Devider = () => {
  return <DeviderContainer></DeviderContainer>;
};

const DeviderContainer = styled.div`
  width: 100%;
  height: 0.5px;
  padding: 0px;
  margin: 0px;
  background-color: rgba(0, 0, 0, 0.2);
`;

export default Devider;
